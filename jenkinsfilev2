pipeline {
   agent any
   triggers {
     githubPush()
   }
      stages {
         stage("Build") {
             steps {
                 sh "chmod +x -R ${env.WORKSPACE}"
                 sh '/var/lib/jenkins/workspace/javaaplication/deploy1.sh'
            }
        }
    }
}

 
 
